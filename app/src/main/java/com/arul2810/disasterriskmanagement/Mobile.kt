package com.arul2810.disasterriskmanagement

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.google.firebase.auth.PhoneAuthProvider
import kotlinx.android.synthetic.main.activity_mobile.*
import java.util.concurrent.TimeUnit

class Mobile : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_mobile)

        //PhoneAuthProvider.getInstance().verifyPhoneNumber(editText2,60,TimeUnit.SECONDS)

    
}
    }

